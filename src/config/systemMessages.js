/** Mensagem fixa para validação do nome ao assinar (não configurável por evento). */
export const NAME_REQUIRED_ALERT =
	'É necessário digitar um nome antes de assinar!';

/** Aviso na lista de demonstração da página inicial (apenas exemplo). */
export const DEMO_ONLY_MESSAGE =
	'Exemplo apenas na página inicial. Para uma lista real, usa o teu link de evento ou cria a tua página em /config.';
